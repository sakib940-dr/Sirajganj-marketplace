# Visitor Features — যা যোগ হলো

শুধু **Visitor-facing** ফিচার যোগ করা হয়েছে। Admin Panel বা Seller Dashboard-এর
কোনো কোড/permission/UI স্পর্শ করা হয়নি।

## ১. Save/Favorite Product

- পণ্যের পেজে ♥ বাটন (আগে থেকেই ছিল) — টগল করে সেভ/আনসেভ করা যায়।
- শুধু লগইন করা ইউজার সেভ করতে পারবেন; লগইন না থাকলে বাটনে ক্লিক করলে Login পেজে পাঠাবে।
- সব সেভ করা পণ্য এখন **"আমার সংরক্ষিত"** পেজে (`/saved`) একসাথে দেখা যায়।

## ২. Saved Sellers (নতুন)

- দোকানের পেজে (`/shop/:slug`) নতুন **"দোকান সেভ করুন"** বাটন যোগ হয়েছে (♥ আইকনসহ), Product Save-এর মতোই টগল-বল আচরণ।
- নতুন টেবিল `shop_saves` (RLS-সুরক্ষিত, শুধু নিজের সেভ নিজে করতে/দেখতে পারবেন)।
- **"আমার সংরক্ষিত"** পেজে সেভ করা পণ্যের নিচে সেভ করা দোকানের তালিকাও দেখা যায়।

## ৩. Related Products (নতুন)

- পণ্যের পেজের একদম নিচে **"সম্পর্কিত পণ্য"** সেকশন — একই ক্যাটাগরির অন্য পণ্য (সর্বোচ্চ ৮টা, বর্তমান পণ্যটি বাদে) দেখায়।
- কোনো একই-ক্যাটাগরি পণ্য না থাকলে সেকশনটাই দেখাবে না (খালি সেকশন দেখাবে না)।

## ৪. Improved Search (বাংলা ⇄ ইংরেজি)

**সমস্যা ছিল:** সার্চ শুধু পণ্যের নামে (বাংলা) হুবহু মিল খুঁজত — ইংরেজি লিখলে
বাংলা নামের পণ্য পাওয়া যেত না ("shirt" লিখলে "শার্ট" নামের পণ্য মিলত না)।

**সমাধান:**
- নতুন টেবিল `search_synonyms` — বাংলা-ইংরেজি সমার্থক শব্দের একটা ডিকশনারি (ক্যাটাগরি, পোশাক, ইলেকট্রনিক্স, খাবার, ঘর সাজানো, স্বাস্থ্য-সৌন্দর্য, বই — প্রায় ৮০+ শব্দজোড়া, migration ফাইলে seed করা)।
- সার্চ করার সময় ইউজারের লেখা শব্দ এই ডিকশনারিতে মিলিয়ে দেখা হয়, মিলে গেলে দুই ভাষার প্রতিশব্দই সার্চে যোগ হয়। যেমন "shirt" লিখলে "শার্ট"-ও খোঁজা হয়, "শাড়ি" লিখলে "saree"-ও (থাকলে) খোঁজা হয়।
- সার্চ এখন পণ্যের **নাম + বিবরণ (description) + ক্যাটাগরির নাম** — তিনটাতেই মিলিয়ে দেখে (আগে শুধু নাম-এ হতো)।
- ডিকশনারিতে না থাকা শব্দের জন্যও স্বাভাবিক (raw) সার্চ কাজ করবে যেমন আগে করত — শুধু কভারেজ বেড়েছে।

> এটি একটা static curated dictionary-ভিত্তিক সমাধান — পুরোপুরি automatic translation/transliteration নয়, তবে মার্কেটপ্লেসের কমন ক্যাটাগরি ও পণ্যের শব্দের জন্য দুই ভাষাতেই কাজ করার মতো যথেষ্ট কভারেজ দেয়। প্রয়োজনে ভবিষ্যতে migration ফাইলে আরও শব্দজোড়া যোগ করা যাবে।

## ৫. Current View Counter (নতুন, সম্পূর্ণ ফ্রন্টএন্ড-সাইড)

- পণ্যের পেজে টাইটেলের নিচে **"এই মুহূর্তে XX জন দেখছেন"** ব্যাজ।
- সংখ্যাটা **১০-৩০ এর মধ্যে র‍্যান্ডম**, এবং কয়েক সেকেন্ড পরপর (৪-৯ সেকেন্ড, র‍্যান্ডম) ছোট ছোট (±৩) পরিবর্তনে ওঠানামা করে — যাতে বাস্তবসম্মত মনে হয়, হুট করে বড় লাফ না দেয়।
- এটি **সম্পূর্ণ ফ্রন্টএন্ড সিমুলেশন** (আসল ভিউ কাউন্ট/অ্যানালিটিক্স নয়) — কোনো ডাটাবেস টেবিল/RPC লাগেনি, তাই Seller Analytics-এর real view_count-এর সাথে এর কোনো সম্পর্ক নেই বা কোনো প্রভাব ফেলে না।

## Database পরিবর্তন — `supabase/migrations/0011_visitor_features.sql`

**Deploy করার সময় এই একটা ফাইল Supabase SQL Editor-এ রান করলেই যথেষ্ট** (idempotent)।

- নতুন টেবিল: `shop_saves` (RLS: প্রতিটা ইউজার শুধু নিজের সেভ insert/select/delete করতে পারবে)।
- নতুন টেবিল: `search_synonyms` (RLS: সবার জন্য শুধু SELECT; কোনো write policy নেই — শুধু migration-এর মাধ্যমে seed করা, Admin/Seller UI থেকে এটা এডিট করার কোনো ব্যবস্থা ইচ্ছাকৃতভাবে যোগ করা হয়নি, কারণ স্কোপ শুধু Visitor ফিচারেই সীমাবদ্ধ)।
- Product Save/Order Click/View Count-এর জন্য আগের migration (`0010_product_analytics.sql`) থাকতে হবে — এটা তার উপরে নির্ভরশীল না, কিন্তু একই প্রজেক্টের অংশ হিসেবে আগে রান করা থাকা উচিত।

## Frontend-এ নতুন/পরিবর্তিত ফাইল

**নতুন:**
- `src/pages/public/SavedItemsPage.jsx` — "আমার সংরক্ষিত" পেজ (Saved Products + Saved Sellers)
- `src/components/shared/CurrentViewersBadge.jsx` — র‍্যান্ডম ওঠানামা করা "এখন দেখছেন" ব্যাজ
- `src/lib/searchSynonyms.js` — বাংলা/ইংরেজি সমার্থক শব্দ fetch + expand লজিক

**পরিবর্তিত:**
- `src/hooks/useProductAnalytics.js` — নতুন `useShopSave()` হুক যোগ হয়েছে (Product Save-এর প্যাটার্ন অনুসরণ করে)
- `src/hooks/useProducts.js` — `useProductSearch()` বাংলা/ইংরেজি সমার্থক শব্দ + description + category মিলিয়ে খোঁজে; নতুন `useRelatedProducts()` হুক
- `src/pages/public/ProductPage.jsx` — Current Viewers ব্যাজ + Related Products সেকশন
- `src/pages/public/ShopPage.jsx` — "দোকান সেভ করুন" বাটন
- `src/constants/routes.js` — `SAVED: "/saved"` রুট যোগ
- `src/routes/AppRoutes.jsx` — `/saved` রুট (লগইন-প্রোটেক্টেড, কোনো নির্দিষ্ট role লাগে না)
- `src/components/layout/Header.jsx` — "সংরক্ষিত" নেভিগেশন লিংক (ডেস্কটপ + মোবাইল মেনু, শুধু লগইন থাকলে দেখাবে)

## যা ছোঁয়া হয়নি

- Admin Panel-এর কোনো পেজ/রাউট/পারমিশন
- Seller Dashboard-এর কোনো পেজ/রাউট/পারমিশন (Product CRUD, Shop Settings, Gallery, Verification, Analytics — সব অপরিবর্তিত)
- বিদ্যমান RLS পলিসি (`products`, `shops`, `profiles` ইত্যাদির উপর আগের policy-গুলো)
- Product Analytics ফিচার (view/save/click count RPC, Seller Analytics পেজ) — আগের মতোই কাজ করবে, নতুন real Save সংখ্যা এখানেও প্রতিফলিত হবে যেহেতু একই `product_saves`/`products.save_count` ব্যবহার হচ্ছে।
