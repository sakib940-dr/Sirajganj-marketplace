# 🆕 Update Notes — Product & Seller System Update

এই আপডেটে যা যা নতুন যোগ হয়েছে, তার সংক্ষিপ্ত ডকুমেন্টেশন। বিস্তারিত সামারি চ্যাটে দেওয়া হয়েছে।

## নতুন Migration
`supabase/migrations/0005_product_seller_update.sql` — **অবশ্যই Supabase SQL Editor-এ রান করতে হবে** (নতুন ও বিদ্যমান উভয় প্রজেক্টেই)। এটি idempotent।

এতে যা আছে:
- `products` টেবিলে `discount_type`, `discount_value` কলাম
- প্রতি দোকানে সর্বোচ্চ ৫০টি পণ্যের DB-level সীমা (trigger)
- প্রতি পণ্যে সর্বোচ্চ ৪টি ছবির DB-level সীমা (trigger)
- `categories` টেবিলে `parent_id` (সাব-ক্যাটাগরি সাপোর্ট) + ১২টি নতুন মূল ক্যাটাগরি ও ৩০টি সাব-ক্যাটাগরি
- নতুন `seller_verifications` টেবিল + RLS পলিসি
- নতুন `seller-verification` স্টোরেজ বাকেট

## মূল ফিচার পরিবর্তন সংক্ষেপে
1. পণ্যের ছবি: সর্বোচ্চ ২০০ KB প্রতি ছবি, ১-৪টি ছবি প্রতি পণ্যে, ভিজিটর পেজে স্বয়ংক্রিয় স্লাইডার।
2. প্রতি দোকানে সর্বোচ্চ ৫০টি পণ্য (UI + DB উভয় জায়গায় সুরক্ষিত)।
3. পণ্যে ডিসকাউন্ট (Fixed/Percentage) — মার্কেটপ্লেসের সব জায়গায় (Product Card, Product Page, Seller/Admin তালিকা) দেখানো হয়।
4. নতুন "সেলার ভেরিফিকেশন" ফর্ম (`/dashboard/verification`) ও অ্যাডমিন রিভিউ পেজ (`/admin/verifications`)।
5. ক্যাটাগরি এখন সাব-ক্যাটাগরি সাপোর্ট করে; আরও ১২টি মূল ক্যাটাগরি ও প্রতিটির অধীনে সাব-ক্যাটাগরি যোগ হয়েছে।

## গুরুত্বপূর্ণ নোট
- `seller-verification` বাকেটটি অন্যান্য মিডিয়া বাকেটের মতোই **public** (প্রকল্পের বিদ্যমান কনভেনশন অনুসরণ করে), তবে ফাইলের URL শুধুমাত্র ডাটাবেসের `seller_verifications` টেবিল থেকে পাওয়া যায়, যেটা RLS দ্বারা সুরক্ষিত (শুধু মালিক ও Super Admin দেখতে পারবেন)। NID-এর মতো স্পর্শকাতর ডকুমেন্টের জন্য ভবিষ্যতে চাইলে এটিকে সম্পূর্ণ প্রাইভেট বাকেটে (signed URL সহ) রূপান্তর করা যেতে পারে।
