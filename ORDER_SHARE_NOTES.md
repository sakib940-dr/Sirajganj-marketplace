# Order System + Share Shop — যা যোগ হলো

শুধু **Order** ও **Share** ফিচার যোগ/আপডেট করা হয়েছে। Admin/Seller-এর অন্য কোনো
ফাংশনালিটি ছোঁয়া হয়নি, এবং বিদ্যমান WhatsApp Order ফ্লো অপরিবর্তিত রাখা হয়েছে
(যেসব দোকানে শুধু WhatsApp নম্বর আছে, তাদের বাটন আগের মতোই একটাই ক্লিকে সরাসরি
WhatsApp-এ যাবে)।

## ১. Order Now বাটন — এখন ৩টা চ্যানেল সাপোর্ট করে

পণ্যের পেজে "অর্ডার করুন" বাটন এখন দোকানের তথ্য অনুযায়ী স্মার্টভাবে আচরণ করে:

| দোকানের কনফিগারেশন | বাটনের আচরণ |
|---|---|
| শুধু WhatsApp নম্বর আছে | **আগের মতোই** — একটা বাটন, ক্লিকে সরাসরি WhatsApp খোলে (prefilled মেসেজসহ) |
| WhatsApp + Facebook Page — দুটোই আছে | বাটনে ক্লিক করলে ড্রপডাউন মেনু খোলে: **হোয়াটসঅ্যাপে অর্ডার করুন**, **ফেসবুক পেজে যোগাযোগ করুন**, এবং (Facebook লিংক থেকে username/ID বের করা গেলে) **ম্যাসেঞ্জারে অর্ডার করুন** |
| শুধু Facebook Page আছে | ড্রপডাউনে Facebook Page + (সম্ভব হলে) Messenger অপশন |
| কিছুই নেই | বাটনটাই দেখাবে না (আগের আচরণের মতো) |

প্রতিটা অপশনে ক্লিক করলে আগের মতোই `click_count` অ্যানালিটিক্স আপডেট হয় (Product
Analytics ফিচার অপরিবর্তিত)।

### "App থাকলে App, না থাকলে Browser" — কীভাবে কাজ করে

এখানে ইচ্ছাকৃতভাবে `wa.me`, `m.me`, ও `facebook.com`-এর মতো ক্যানোনিক্যাল
`https://` লিংক ব্যবহার করা হয়েছে — কোনো কাস্টম URL scheme (`whatsapp://`,
`fb://`) বা "প্রথমে scheme try করো, তারপর টাইমার দিয়ে fallback করো" এই ধরনের
hack ব্যবহার করা হয়নি। কারণ:

- এই তিনটা ডোমেইনই WhatsApp/Meta নিজেরাই **iOS Universal Links** ও **Android
  App Links** হিসেবে আগে থেকেই রেজিস্টার করে রেখেছে।
- তাই মোবাইলে এসব লিংকে ট্যাপ করলে **OS নিজে থেকেই** ইনস্টল করা অ্যাপ থাকলে সেটা
  খুলে দেয়, না থাকলে স্বাভাবিকভাবে ব্রাউজারে/ওয়েব ভার্সনে খুলে যায় — ঠিক যা
  চাওয়া হয়েছে, কোনো এক্সট্রা কোড ছাড়াই।
- পুরনো "কাস্টম scheme + setTimeout fallback" পদ্ধতিটা iOS Safari, পপআপ
  ব্লকার, এবং Facebook/Instagram-এর in-app ব্রাউজারে প্রায়ই ব্যর্থ হয় — তাই
  production-ready সমাধান হিসেবে এটা এড়ানো হয়েছে।

### Facebook Messenger লিংক কীভাবে তৈরি হয়

দোকানের সেটিংসে যে `facebook_link` (Facebook Page URL) দেওয়া থাকে, তা থেকে
username/ID পার্স করে `https://m.me/<id>` লিংক বানানো হয়। যদি লিংকটা থেকে
আইডেন্টিফায়ার বের করা না যায় (যেমন কোনো অস্বাভাবিক/ভাঙা URL), তাহলে
Messenger অপশনটা মেনুতে দেখাবে না (ভাঙা লিংক দেখানোর চেয়ে না-দেখানো ভালো)।

**নতুন ফাইল:**
- `src/lib/orderChannels.js` — চ্যানেল-লিংক বিল্ডিং লজিক + Facebook URL পার্সার
- `src/components/shared/OrderNowMenu.jsx` — বাটন/ড্রপডাউন কম্পোনেন্ট
- `src/components/ui/dropdown-menu.jsx` — shadcn-স্টাইল Dropdown Menu wrapper (Radix UI-এর উপর ভিত্তি করে; আগে থেকেই `package.json`-এ dependency ছিল, শুধু wrapper ফাইলটা তৈরি করা হলো)

**পরিবর্তিত:**
- `src/pages/public/ProductPage.jsx` — পুরনো inline WhatsApp/Facebook লজিক সরিয়ে `<OrderNowMenu />` বসানো হয়েছে

## ২. Share Shop — Seller ও Visitor উভয়ের জন্য

নতুন **"শেয়ার করুন"** বাটন যোগ হয়েছে দুই জায়গায়:
- **Visitor-facing** দোকানের পেজে (`/shop/:slug`) — Save বাটনের পাশে
- **Seller Dashboard**-এর "দোকানের তথ্য" পেজে (`/dashboard/shop`) — যেখানে সেলার নিজের দোকানের লিংক দেখতে পান, সেখানে "দেখুন" বাটনের পাশে

### কীভাবে কাজ করে

- **Web Share API** (`navigator.share()`) ব্যবহার করা হয়েছে — এটাই মোবাইলের
  আসল **নেটিভ শেয়ার শিট** খোলে (WhatsApp, Facebook, Messenger, SMS, Email,
  এবং ইনস্টল করা অন্য যেকোনো শেয়ার-সাপোর্টেড অ্যাপ সহ)।
- শেয়ার করা হয় দোকানের **plain absolute https লিংক** (`origin + /shop/slug`)
  সাথে দোকানের নাম ও একটা ছোট বার্তা — যেহেতু এটা একটা সাধারণ ওয়েব লিংক, তাই
  WhatsApp/Facebook/Messenger সহ সব অ্যাপেই স্বাভাবিকভাবে খোলে ও কাজ করে,
  কোনো বিশেষ ফরম্যাটিং লাগে না।
- ডেস্কটপ ব্রাউজারে (যেখানে `navigator.share` সাপোর্ট নেই) বাটনে ক্লিক করলে
  লিংকটা **ক্লিপবোর্ডে কপি** হয়ে যায় ও সংক্ষিপ্ত সময়ের জন্য "লিংক কপি হয়েছে"
  দেখায় — যাতে ডেস্কটপ থেকেও কাজটা করা যায়।
- ইউজার শেয়ার ডায়ালগ বাতিল করলে (`AbortError`) নিরবে কিছু না করেই ফিরে আসে —
  কোনো error দেখায় না।

**নতুন ফাইল:**
- `src/components/shared/ShareShopButton.jsx`

**পরিবর্তিত:**
- `src/pages/public/ShopPage.jsx` — Save বাটনের পাশে `<ShareShopButton />`
- `src/pages/seller/ShopSettingsPage.jsx` — "দেখুন" বাটনের পাশে `<ShareShopButton variant="ghost" />`

> **নোট:** শেয়ার করা লিংকের সাথে rich preview (থাম্বনেইল/টাইটেল কার্ড) দেখাতে
> হলে পেজে dynamic Open Graph meta ট্যাগ (og:title, og:image ইত্যাদি) লাগবে,
> যা এই প্রজেক্টে কোথাও এখনো সেটআপ করা নেই (কোনো পেজেই dynamic `<head>` মেটাডেটা
> নেই)। এটা একটা আলাদা, বড় স্কোপের কাজ (SEO/meta ইনফ্রাস্ট্রাকচার), তাই এই
> টাস্কের বাইরে রাখা হয়েছে — লিংকটা নিজে সব জায়গাতেই সঠিকভাবে খুলবে ও কাজ করবে,
> শুধু শেয়ার করার সময় প্রিভিউ কার্ডে হয়তো জেনেরিক/খালি থাম্বনেইল দেখাতে পারে।

## Database পরিবর্তন

**কোনো নতুন মাইগ্রেশন লাগেনি।** দুটো ফিচারই বিদ্যমান কলাম/টেবিল
(`shops.whatsapp_number`, `shops.facebook_link`, `shops.slug`) ব্যবহার করে —
কোনো স্কিমা পরিবর্তন দরকার হয়নি।

## যা ছোঁয়া হয়নি

- Admin Panel-এর কোনো পেজ/রাউট/পারমিশন
- Seller Dashboard-এর অন্য কোনো পেজ (Product CRUD, Gallery, Verification, Analytics)
- Product Analytics-এর click/view/save ট্র্যাকিং লজিক (শুধু ব্যবহার করা হয়েছে, পরিবর্তন করা হয়নি)
- ShopPage-এর "যোগাযোগ" সেকশনে থাকা আলাদা WhatsApp/Facebook লিংক (এগুলো Order বাটন না, দোকানের ইনফো কার্ডের সাধারণ কন্টাক্ট লিংক — অপরিবর্তিত রাখা হয়েছে)
- যেসব দোকানে শুধু WhatsApp নম্বর আছে, তাদের জন্য Order বাটনের ক্লিক-বিহেভিয়ার (single-click direct WhatsApp) হুবহু আগের মতোই আছে
